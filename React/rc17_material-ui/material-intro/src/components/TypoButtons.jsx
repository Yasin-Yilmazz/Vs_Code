import { Typography } from "@mui/material";
const TypoButtons = () => {
  return (
    <div>
      <Typography
        variant="h6"
        component="h2"
        align="center"
        color="pink"
        sx={{ background: "#ddd" }}
      >
        Typography, Buttons, Conainer, Box
      </Typography>
    </div>
  );
};

export default TypoButtons;
